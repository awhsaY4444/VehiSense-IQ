{
  "name": "vehisense-iq",
  "private": true,
  "version": "1.0.0",
  "description": "Edge AI-powered fleet maintenance decision intelligence platform.",
  "scripts": {
    "dev": "docker compose up --build",
    "frontend": "npm --prefix frontend run dev",
    "backend": "uvicorn backend.app.main:app --reload --host 0.0.0.0 --port 8000"
  },
  "workspaces": [
    "frontend"
  ]
}
