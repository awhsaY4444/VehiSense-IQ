# Backend / Render
APP_ENV=development
DATABASE_URL=postgresql+psycopg://vehisense:vehisense@localhost:5432/vehisense
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# Optional: allow Vercel preview deployments such as https://branch-project.vercel.app
# CORS_ORIGIN_REGEX=https://.*\.vercel\.app

# Frontend / Vercel
# Local development can omit this and will use http://localhost:8000 in Vite dev mode only.
# Production must set this to the Render backend URL.
VITE_API_URL=https://vehisense-iq-api.onrender.com
